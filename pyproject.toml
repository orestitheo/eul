[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eul"
version = "0.2.0"
requires-python = ">=3.10"

[project.scripts]
eul-evolve = "eul.evolve:main"

[tool.setuptools.packages.find]
where = ["src"]
